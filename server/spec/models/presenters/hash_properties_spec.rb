require 'spec_helper'

describe Presenters::HashProperties do
  let(:fakeModel) do

  end

  describe "hash_value" do
    # yield self
    # return self.data_hash
  end

  describe "add(key,propname=nil)" do
    # propname ||= key
    # prop = self.model.send propname
    # self.set(key,prop)
  end

  describe "set(key,value)" do
    # (self.data_hash[key] = value) unless value.nil?
  end



end