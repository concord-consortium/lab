/*global define: false */

define(function() {

  return {
    interactiveUrl: {
      defaultValue: ''
    },
    velocityVectors: {
      defaultValue: {
        color: "#000",
        width: 0.01,
        length: 2
      }
    },
    forceVectors: {
      defaultValue: {
        color: "#169C30",
        width: 0.01,
        length: 2
      }
    }
  };
});
