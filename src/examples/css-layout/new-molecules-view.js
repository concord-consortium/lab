/*globals $ d3 Lab */
/*jshint eqnull: true*/

Lab = Lab || {};

Lab.newMoleculesView = function(node, model, opts) {

  var processOptions = function() {
  },

  render = function() {
  },

  prepareDOM = function() {
  },

  renderGrid = function() {
  },

  renderTicks = function() {
  },

  renderMolecules = function() {
  };

  processOptions();

  return view = {
    render: function() {
      prepareDOM();

      renderGrid();
      renderTicks();
      renderMolecules();
    },

    update: function() {
    }
  };

};
