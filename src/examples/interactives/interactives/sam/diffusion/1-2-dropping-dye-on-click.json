{
  "title": "Basic Diffusion on click",
  "publicationStatus": "draft",
  "subtitle": "Watch a drop of dye spreading out in water.",
  "about": "Click in the model to add a drop of dye.  Watch how the molecules move through the water.  Trace a random individual molecule to see how it moves through the liquid.",
  "models": [
    {
      "id": "model1",
      "url": "/imports/legacy-mw-content/converted/sam-activities/diffusion/page1/page1$0.json",
      "viewOptions": {
        "controlButtons": "play_reset",
        "imageMapping": {
          "waterDyeKey.png": "waterDyeKey.svg"
        }
      },
      "onLoad": [
        "onClick('plot', function (x, y) {",
        "  batch(function() {",
        "    var atomsToMark = atomsWithin(x, y, 0.5);",
        "    if (atomsToMark != -1) {",
        "      markAtoms(atomsToMark);",
        "    }",
        "  });",
        "  start();",
        "});"
      ]
    }
  ],
  "parameters": [
    {
      "name": "tracedAtom",
      "initialValue": -99,
      "onChange": ""
    },
    {
      "name": "wasMarked",
      "initialValue": 0,
      "onChange": ""
    }
  ],
  "components": [
    {
      "type": "button",
      "id": "trace",
      "text": "Trace Random Molecule",
      "action": [
        "var opt = {supressRepaint: true};",
        "var atomID = randomAtoms(1);",
        "var tracedAtomID = get('tracedAtom');",
        "if (tracedAtomID != -99) {",
          "setAtomProperties(tracedAtomID, {element: 0, marked: get('wasMarked')}, false, false, opt);",
        "}",
        "setAtomProperties(atomID, {element: 1}, false, false, opt);",
        "set({tracedAtom: atomID});",
        "set({wasMarked: getAtomProperties(atomID).marked});",
        "setAtomProperties(atomID, { marked: 0}, false, false, opt);",
        "traceAtom(atomID);"
      ]
    }
  ],
  "layout": {
    "under-model": [
      "trace"
    ]
  },
  "template": [
    {
      "id": "under-model",
      "top": "model.bottom",
      "left": "model.left",
      "width": "model.width",
      "padding-top": "0.25em",
      "align": "center"
    }
  ]
}